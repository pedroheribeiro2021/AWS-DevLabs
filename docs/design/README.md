# Visual identity — candidates

Not decided yet. Parking two AI-generated logo candidates here (2026-09-16) so they survive past the Downloads folder, for whenever we get to Phase 10 (Refinement) / actual UI design work.

- `logo-option-1-flat.png` — flask + `</>` mark, flat navy/orange, no gradients.
- `logo-option-2-glossy.png` — same concept (flask + `</>`), glossy/3D rendering with gradients and an "eye" shape framing it.

## Note

Planejamento.md (section 27) explicitly asks for a clean, technical look and calls out **avoiding excessive gradients**. Option 1 matches that direction more closely; option 2 leans more generic-AI-glossy. Not a final call — just flagging it for whoever (human or Claude) picks this up later. Both are AI-generated placeholders ("Made with AI" watermark) — will need a real pass (or at least watermark removal / redraw) before shipping either.
